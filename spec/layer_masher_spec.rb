require 'spec_helper'

describe LayerMasher do
  it 'has a version number' do
    expect(LayerMasher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
